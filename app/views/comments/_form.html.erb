<%= form_for(@comment) do |f| %>
    <% if @comment.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

          <ul>
            <% @comment.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="clearfix">
      <%= f.label :Comment %>
      <div class="input"> <%= f.text_area :text, :class=>"input-xxlarge", :rows=>"10" %></div>
    </div>
    <%= f.hidden_field :tutorial_id, :value=>@tutorial.id if @tutorial %>
    <div class="clearfix">
      <div class="input large-submit-buttons"><%= f.submit 'Post', :class=>"btn primary" %></div>
    </div>
<% end %>
